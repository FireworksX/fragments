import { GraphState } from "@graph-state/core";
import { useStyleSheet } from "@/shared/hooks/useStyleSheet";

export const useGlobalStyles = (globalManager: GraphState) => {
  // const { addLayerStyle } = useStyleSheet(fragmentManager);
};
