import styles from './styles.module.css'
import { Container } from '@/shared/ui/Container'

export const CampaignsListPage = () => {
  return <Container className={styles.root}>Choose campaign or create new</Container>
}
