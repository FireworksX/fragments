mutation CreateCampaign($areaId: Int!, $name: String!, $active: Boolean!, $weight: Float!) {
    createCampaign(cmp: {areaId: $areaId,name: $name, active: $active, archived: true }) {
        id
        name
        active
    }
}
