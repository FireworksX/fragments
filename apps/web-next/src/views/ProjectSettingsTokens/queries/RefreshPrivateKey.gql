mutation RefreshPrivateKey($projectId: Int!) {
    changeProjectPrivateKey(projectId: $projectId) {
        id
        privateKey {
            id
            name
            value
        }
    }
}
