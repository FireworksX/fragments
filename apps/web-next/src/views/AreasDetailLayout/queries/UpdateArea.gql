mutation UpdateArea($id: Int!, $name: String, $description: String, $code: String) {
    updateArea(area: { id: $id, name: $name, description: $description, areaCode: $code }) {
        id
        name
        description
    }
}
