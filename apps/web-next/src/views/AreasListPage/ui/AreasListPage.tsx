import styles from './styles.module.css'
import { Container } from '@/shared/ui/Container'

export const AreasListPage = () => {
  return <Container className={styles.root}>Choose area or create new</Container>
}
