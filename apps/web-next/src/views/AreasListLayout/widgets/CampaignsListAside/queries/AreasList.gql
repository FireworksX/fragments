query AreasList($projectId: Int!) {
    area(projectId: $projectId) {
        id
        name
        description
        defaultCampaign {
            logo {
                url: publicPath
            }
            active
        }
    }
}
