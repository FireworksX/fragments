query Campaign($areaId: Int, $id: Int) {
    campaign(areaId: $areaId, campaignId: $id) {
        id
        name
        active
        logo {
            url: publicPath
        }
        default
    }
}
