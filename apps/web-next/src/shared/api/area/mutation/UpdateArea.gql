mutation UpdateArea($id: Int!, $description: String, $code: String, $properties: [JSON!]) {
    updateArea(area: { id: $id, description: $description, areaCode: $code, properties: $properties }) {
        id
#        name
        description
        properties
    }
}
